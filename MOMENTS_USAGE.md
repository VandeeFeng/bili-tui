# Moments 功能使用说明

## 功能概述
Moments 功能可以显示你关注的B站UP主的最新动态，包括视频发布、文字动态等内容。

## 使用步骤

### 1. 启动应用
```bash
source ~/.bashrc
cargo run
```

### 2. 进入Moments模式
1. 按 `:` 键打开命令模式
2. 输入 `moments` (或简写 `m`)
3. 按回车键执行

### 3. 界面说明
启动moments功能后，界面会分为两个面板：

**左侧面板 - 关注作者列表**
- 显示你关注的所有UP主
- 格式：`👤 作者名称 (UID: xxxxx)`

**右侧面板 - 作者动态详情**
- 显示选中作者的最新动态
- 包含视频信息、文字内容、互动数据等

### 4. 操作指南

#### 导航操作
- `j` / `k` - 在作者列表中上下移动
- `↑` / `↓` - 同样可以在作者列表中导航
- `Tab` - 在两个面板之间切换焦点

#### 退出操作
- `q` - 退出moments模式，返回主界面
- `Esc` - 同样退出moments模式

#### 其他操作
- `:` - 在moments模式下也可以打开命令模式
- `?` - 显示帮助信息

## 功能特性

### 动态加载
- 选中作者后会自动加载其最新动态
- 显示加载状态和进度信息

### 丰富内容显示
- **视频动态**：显示标题、时长、播放量、点赞数等
- **文字动态**：显示发布内容
- **时间戳**：显示发布时间
- **互动数据**：显示点赞、评论、转发数量

### 错误处理
- 如果SESSDATA未设置，会显示配置指南
- API调用失败时会显示错误信息
- 网络问题会给出相应提示

## 故障排除

### 问题：无法加载moments数据
**解决方案：**
1. 确保SESSDATA环境变量已正确设置
2. 检查网络连接
3. 确认B站账号登录状态

### 问题：TUI界面无法启动
**解决方案：**
1. 确保终端支持TUI显示
2. 检查是否有其他程序占用终端
3. 尝试在支持的终端中运行（如gnome-terminal、konsole等）

### 问题：动态内容显示不完整
**解决方案：**
1. 这是正常现象，界面只显示前10条动态
2. 可以通过j/k键浏览不同作者查看更多内容

## API说明

Moments功能使用两个B站API：

1. **关注列表API** - 获取关注的作者列表
2. **用户动态API** - 获取指定用户的最新动态

两个API都需要SESSDATA认证，请确保从浏览器正确获取并设置。